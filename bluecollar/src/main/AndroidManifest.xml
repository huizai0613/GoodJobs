<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.goodjobs.bluecollar">

    <application
        android:allowBackup="true"
        android:label="@string/app_name">
        <activity
            android:name=".activity.BlueCollarActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"></activity>

        <activity
            android:name=".activity.ItemSettingActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"></activity>

        <activity
            android:name=".activity.ItemMoreActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"></activity>

        <activity
            android:name=".activity.ItemResumeActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"></activity>

        <activity
            android:name=".activity.ResumeMoreActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"></activity>

        <activity
            android:name=".activity.BlueSearchActivity"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".activity.BlueJobSearchResultActivity"
            android:screenOrientation="portrait"></activity>
    </application>

</manifest>
